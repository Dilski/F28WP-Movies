
/*Border box*/
* {
    box-sizing: border-box;
}

html, body, div, object, iframe, h1, h2, h3, h4, h5, h6, p, blockquote, ol, ul, li, form, legend, label, table, header, footer, nav, section, figure {
    margin: 0;
    padding: 0; }

header, footer, nav, section, article, figure {
    display: block; }

/*align*/
.right {
    float: right;
    padding: 0 0 1em 1em;
}
.left {
    float: left;
    padding: 0 1em 1em 0;
}

.text-right {
    text-align: right;
}

.text-left {
    text-align: left;
}

.text-center {
    text-align: center;
}
.centered {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.middle {
  position: float;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.valid {
    color: #87D37C;
}

.error {
    color: #EC644B; }

.warning {
    color: #F5D76E; }

.information {
    color: #52B3D9; }

.color-block {
    border-radius: 8px;
    display: block;
    padding: 1rem;
    line-height: 1rem;
    width: auto;
}

body {
    background: #e0e0e0;
    font-family: 'Roboto', sans-serif;
    color: #1f1f1f;
}

a {
    color: #1f1f1f;
    text-decoration: none;
    outline: 0;
}

.second-font {
    font-family: 'Open Sans', sans-serif;
}

.btn {
    border-radius: 3px;
    padding: 1em 1em;
    background: #6BB9F0;
    text-decoration: none;
    border: 0;
    color: white;
    font-weight: bold;
    font-size: 0.8rem;
    display: inline-block;
}

.btn-valid {background: #87D37C;}
.btn-error {background: #EC644B;}
.btn-warning {background: #F5D76E; color: black}
.btn-information {background: #52B3D9;}

input, textarea {
    border: 1px solid #606060;
    border-radius: 3px;
    padding: 0.7rem;
    font-family: inherit;
    font-size: 100%;
}

button, input, select {
    font-family: inherit;
    font-size: 100%;
    margin: 0px;
}

img, video, object {
    max-width: 100%;
    height: auto;
}

.img-rounded {
    border-radius: 50%;
}
table { 
  width: 100%; 
  border-collapse: collapse; 
}
th { 
  background: #333; 
  color: white; 
  font-weight: bold; 
}
td, th { 
  padding: 6px; 
  border: 1px solid #ccc; 
  text-align: left; 
}

nav {
    background-color: #1f1f1f;
    margin: 0;
    padding: 0;
    overflow: hidden;
    list-style-type: none;
}

nav a {
    float: left;
    display: block;
    color: white;
    text-align: center;
    padding: 14px 16px;
    text-decoration: none;
}
